# Compatibility shim for tmux < 3.2 (which doesn't check XDG paths).
# All configuration lives in ~/.config/tmux/tmux.conf.
if-shell '[ -f ~/.config/tmux/tmux.conf ]' 'source-file ~/.config/tmux/tmux.conf'
